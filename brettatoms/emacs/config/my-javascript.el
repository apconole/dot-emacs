;
; Javascript configs
;

; NOTE: we mostly just use the js2-mode included with nxhtml mode
;; (autoload 'js2-mode "js2" nil t)
;; (add-to-list 'auto-mode-alist '("\\.js$" . js2-mode))
(provide 'my-javascript)